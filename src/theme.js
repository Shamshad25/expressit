import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#194f92",
      light: "#cbd5f0",
    },
    secondary: {
      main: "#efb7ba",
      light: "#e5c0c8",
    },
    tertiary: {
      main: "#fafafb",
    },
  },
});
